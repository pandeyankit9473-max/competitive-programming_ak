#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

void solve() {
    int n; cin>>n;
    vector<int>a(2*n);
    for(auto&it:a) cin>>it;
     ll maxi=0;
    while(a.size()!=0){
        int l=0, r=0;
        int ans=0;
        while(r<)

        }
    }
}

int main() {

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    while(t--) {
        solve();
    }

    return 0;
}